function infoUser(el) {
    let info = document.getElementById(el);
    info.classList.toggle('activeInfo');
}

function setaInfo(el) {
    let seta = document.getElementById(el);
    seta.classList.toggle('active')
}